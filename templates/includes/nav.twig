<!-- Sidebar Wrapper -->
<div class="sidebar-wrapper" data-simplebar="true">
    <!-- Sidebar Header with Violet Gradient -->
    <div class="sidebar-header" style="background: linear-gradient(135deg, #7C4DFF, #9C27B0); padding: 1.25rem 1rem;">
        <div class="sidebar-logo">
            <span style="font-size: 1.75rem;">🐾</span>
            <span class="logo-text" style="color: white; font-weight: 600;">Tierphysio</span>
        </div>
        <div class="toggle-icon ms-auto d-lg-block d-none" style="color: white; cursor: pointer;">
            <i class='bx bx-arrow-back'></i>
        </div>
    </div>

    <!-- Sidebar Navigation -->
    <nav class="main-nav">
        <a href="/dashboard.php" class="{{ current_page == 'dashboard' ? 'active' : '' }}">
            <i class='bx bx-home-alt'></i>
            <span>Dashboard</span>
        </a>
        <a href="/patients.php" class="{{ current_page == 'patients' ? 'active' : '' }}">
            <i class='bx bx-group'></i>
            <span>Patienten</span>
        </a>
        <a href="/appointments.php" class="{{ current_page == 'appointments' ? 'active' : '' }}">
            <i class='bx bx-calendar'></i>
            <span>Termine</span>
        </a>
        <a href="/notes.php" class="{{ current_page == 'notes' ? 'active' : '' }}">
            <i class='bx bx-note'></i>
            <span>Notizen</span>
        </a>
        <a href="/invoices.php" class="{{ current_page == 'invoices' ? 'active' : '' }}">
            <i class='bx bx-receipt'></i>
            <span>Rechnungen</span>
        </a>
        <a href="/owners.php" class="{{ current_page == 'owners' ? 'active' : '' }}">
            <i class='bx bx-user'></i>
            <span>Besitzer</span>
        </a>
        <a href="/accounting.php" class="{{ current_page == 'accounting' ? 'active' : '' }}">
            <i class='bx bx-calculator'></i>
            <span>Buchhaltung</span>
        </a>
        <a href="/settings.php" class="{{ current_page == 'settings' ? 'active' : '' }}">
            <i class='bx bx-cog'></i>
            <span>Einstellungen</span>
        </a>
        {% if is_admin %}
        <a href="/admin/dashboard.php" class="{{ current_page == 'admin' ? 'active' : '' }}">
            <i class='bx bx-shield'></i>
            <span>Admin Bereich</span>
        </a>
        {% endif %}
    </nav>

    <!-- Sidebar Footer -->
    <div class="sidebar-footer" style="margin-top: auto; padding: 1rem; border-top: 1px solid rgba(124, 77, 255, 0.1);">
        <small class="text-muted">Version {{ app_version|default('3.0.0') }}</small>
    </div>
</div>

<!-- Sidebar Overlay for Mobile -->
<div class="sidebar-overlay"></div>