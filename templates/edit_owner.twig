{% extends "layout.twig" %}

{% block content %}
<div class="container py-4">

    <h2>{% if owner %}👤 Besitzer bearbeiten{% else %}➕ Neuer Besitzer{% endif %}</h2>

    {% if errors %}
    <div class="alert alert-danger">
        <ul>
            {% for e in errors %}
            <li>{{ e }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}

    <form method="post" class="bg-dark p-3 rounded shadow">
        {{ csrf_field()|raw }}
        <div class="row">
            <div class="col-md-6 mb-3">
                <label class="form-label">Vorname</label>
                <input type="text" name="firstname" class="form-control" value="{{ owner.firstname|default('') }}" required>
            </div>
            <div class="col-md-6 mb-3">
                <label class="form-label">Nachname</label>
                <input type="text" name="lastname" class="form-control" value="{{ owner.lastname|default('') }}" required>
            </div>
        </div>

        <div class="mb-3">
            <label class="form-label">Straße & Hausnummer</label>
            <input type="text" name="street" class="form-control" value="{{ owner.street|default('') }}">
        </div>

        <div class="row">
            <div class="col-md-4 mb-3">
                <label class="form-label">PLZ</label>
                <input type="text" name="zipcode" class="form-control" value="{{ owner.zipcode|default('') }}">
            </div>
            <div class="col-md-8 mb-3">
                <label class="form-label">Ort</label>
                <input type="text" name="city" class="form-control" value="{{ owner.city|default('') }}">
            </div>
        </div>

        <div class="mb-3">
            <label class="form-label">Telefon</label>
            <input type="text" name="phone" class="form-control" value="{{ owner.phone|default('') }}">
        </div>

        <div class="mb-3">
            <label class="form-label">E-Mail</label>
            <input type="email" name="email" class="form-control" value="{{ owner.email|default('') }}">
        </div>

        <div class="mb-3">
            <label class="form-label">Notizen</label>
            <textarea name="notes" class="form-control">{{ owner.notes|default('') }}</textarea>
        </div>

        <button type="submit" class="btn btn-success">💾 Speichern</button>
        <a href="patients.php" class="btn btn-secondary">Zurück</a>
    </form>
</div>
{% endblock %}