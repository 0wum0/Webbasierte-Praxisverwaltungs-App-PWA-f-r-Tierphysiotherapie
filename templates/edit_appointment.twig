{% extends "layout.twig" %}

{% block content %}
<div class="container py-4">

    <h2>
        {% if appointment %}📅 Termin bearbeiten{% else %}➕ Neuer Termin{% endif %}
        {% if patient %} für {{ patient.name }}{% endif %}
    </h2>

    {% if errors %}
    <div class="alert alert-danger">
        <ul>
            {% for e in errors %}
            <li>{{ e }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}

    <form method="post" class="bg-dark p-3 rounded shadow">
        <div class="mb-3">
            <label class="form-label">Datum & Uhrzeit</label>
            <input type="datetime-local" name="appointment_date" class="form-control"
                   value="{{ appointment.appointment_date|default('') }}" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Dauer (Minuten)</label>
            <input type="number" name="duration" class="form-control"
                   value="{{ appointment.duration|default(60) }}" min="15" step="15">
        </div>

        <div class="mb-3">
            <label class="form-label">Notizen</label>
            <textarea name="notes" class="form-control">{{ appointment.notes|default('') }}</textarea>
        </div>

        <button type="submit" class="btn btn-success">💾 Speichern</button>
        <a href="patient.php?id={{ patient.id }}" class="btn btn-secondary">Zurück</a>
    </form>
</div>
{% endblock %}